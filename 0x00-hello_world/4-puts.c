#include <stdio.h>
/**
 * main - function
 * puts:  prints the string inside it
 * Return: int 0 if there are no errors
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
