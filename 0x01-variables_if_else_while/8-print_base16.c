#include <stdio.h>

/**
* main - Print the numbers in hexidecimal
*
* Return 0 
*/
int main(void)
{
		int integer;

		for (integer = '0'; integer <= '9'; integer++)
		putchar(digit);
		for (integer = 'a'; integer <= 'f'; integer++)
		putchar(digit);
		putchar('\n');
	return (0);
}
